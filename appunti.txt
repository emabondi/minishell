___________________________________________________________
creare più mini dentro mini

fixare *

fare le redirection con heredoc
___________________________________________________________


pid

pipe

tutti gli errori vanno stampati su STDERR	(fd 2)

$$

FATTO-- $? cambiare exit status    da controllare
		{
			exit 3 -> exit status = 3
			exit l -> exit status = 255
			exit 3 3
			exit 333
		}

provare: gdsghs | gfsbgsb tees TETS| teesghghte sgh hteS|gg tetsht e

FATTO--echo '$HOME' && "$HOME" non devono stampare le virgolette
	= trovare un modo per gestire le virgolette in generale

FATTO--echo '$HOME' && "$HOME" non devono stampare le virgolette = trovare un modo per gestire le virgolette in generale
FATTO--echo con stringhe con più spazi consecutivi, gli spazi vengono eliminati dallo split = tokenizer?
FATTO--se come comando viene passata una directory -> es. $HOME -> bash: /Users/ebondi: is a directory
FATTO--i nomi delle variabili di export devono contenere almeno una lettera e solo caratteri alfanumerici

lo chiamiamo minisburo o minishell?

echo -n -n -n ciao -n -n
es var. a="ls -la" , $a esegue il comando

ad export e unset si possono passare piu variabili da creare/modificare
export deve aggiornare le variabili gia esistenti anche con caratteri speciali es.][


alla fine pulire il makefile