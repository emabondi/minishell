--da testare il caso di variabile vuota per ft_get_env_var
	(post export)

export argomento=""
export argomento=valore
export argomento
export lol -> con env: non compare -> con export lol
export lol= -> con env: lol= -> con export lol=""
export lol=ciao / lol="ciao" -> con env: lol=ciao -> con export lol="ciao"
export *qualcosa non alfnum* -> errore not a valid identifier -> non é un errno
export *variabile giá esistente* -> sostituisce il valore e non duplica
export *variabile* (variabile esiste e ha un valore) -> non fa niente


check expand con "$HOME" //FATTO
fix get_env_var con "$HOM" o similari //FATTO

$? cambiare exit status
{
	exit 3 -> exit status = 3
	exit l -> exit status = 255
}

questo comando fa segfault: "| |"
provare: gdsghs | gfsbgsb tees TETS| teesghghte sgh hteS|gg tetsht e

cd e basta va in home

alla fine pulire il makefile